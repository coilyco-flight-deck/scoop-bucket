# Catalog metadata for the cross-repo knowledge graph. Dev verbs moved to the
# justfile; this path survives because check_catalog_block pins it.

catalog:
  description: "Scoop bucket for Windows binaries published by coilyco-flight-deck, with automated manifest updates from the release trains."
  dependsOn: []
